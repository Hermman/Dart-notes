/*
  Set 类型
    
   主要功能： 用于去除数组中重复内容（去重）

   Set是没有顺序 并且 不能重复的集合
   所以不能通过索引去获取值

*/
void main() {

  // var s = new Set();
  // s.add('a');
  // s.add('b');
  // print(s);             // {a, b}
  // s.add('a');
  // s.add('c');
  // print(s);            // {a, b, c}
  // print(s.toList());   // [a, b, c]

  /* 数组去重 */
  // List list1 = ['a', 'b', 'c', 'a', 'd', 'c'];
  // var s1 = new Set();
  // s1.addAll(list1);
  // print(s1);           // {a, b, c, d}
  // print(s1.toList());  // [a, b, c, d]


}